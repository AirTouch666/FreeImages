import clientConfig from './client';
import { ConfigType, StorageConfigType, AppConfigType } from './config.template';

// 导出客户端配置管理器
export default clientConfig;

// 导出类型
export type { ConfigType, StorageConfigType, AppConfigType }; 